/** Feature flags for temporarily gated product surfaces. */
export const FEATURE_FLAGS = {
  /** Registro / crear cuenta deshabilitado hasta arreglar el flujo de correo. */
  authSignupEnabled: false,
} as const
