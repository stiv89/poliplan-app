# For the Supabase CLI. https://supabase.com/docs/guides/cli/config
project_id = "sfyovljgzriaquguygte"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:5173"

# Correos de Auth vía Brevo (Edge Function auth-send-email).
# En producción: Dashboard → Authentication → Hooks → Send Email
# Secrets: BREVO_API_KEY, APP_URL, EMAIL_FROM, SEND_EMAIL_HOOK_SECRET
#
# [auth.hook.send_email]
# enabled = true
# uri = "http://host.docker.internal:54321/functions/v1/auth-send-email"
# secrets = "SEND_EMAIL_HOOK_SECRET"
